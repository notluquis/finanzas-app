# Backend API
PORT=4000

# Credenciales base de datos (HostGator / cPanel)
DB_HOST=localhost
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=

# Autenticación y bootstrap
JWT_SECRET=CAMBIA_ESTA_SECRETA
ADMIN_EMAIL=
ADMIN_PASSWORD=
# ADMIN_EMAIL y ADMIN_PASSWORD se usan una sola vez para crear el usuario "Dios" si la tabla está vacía.
